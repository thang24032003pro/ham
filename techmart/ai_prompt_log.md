prompt 1: hãy giải thích nguyên lý single responsibility srp trong lập trình bằng một ví dụ dễ hiểu liên quan đến các bộ phận trong một nhà hàng thức ăn nhanh

prompt 2: tôi định thiết kế hàm tính toán áp dụng mã giảm giá và tính thuế nhận tham số đầu vào là tổng tiền và loại khách hàng theo bạn tôi có nên tách chúng làm hai hàm độc lập hay gộp chung để tuân thủ nguyên tắc srp tốt hơn

prompt 3: hàm xử lý đơn hàng của tôi đang trả về kết quả sai số tiền tôi nghi ngờ là một hàm tính giảm giá bên trong chưa có từ khóa return làm sao để debug kiểm tra giá trị trả về của một hàm trong javascript